# This directory is used for admin-uploaded images (via /api/upload).
# On Render free tier, uploaded files do NOT persist across restarts.
# Use image URLs (Google Drive, Imgur, Cloudinary, etc.) for permanent gallery images.
